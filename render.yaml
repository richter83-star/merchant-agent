services:
  - type: web
    name: merchant-agent
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python web_ui.py"
    envVars:
      - key: GUMROAD_ACCESS_TOKEN
        sync: false
      - key: OPENAI_API_KEY
        sync: false
